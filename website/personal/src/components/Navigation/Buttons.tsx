import { Button } from "../Button";

// type buttonsProps = {};

export const Buttons = () => (
	<span className="inline-flex gap-2">
		<Button className="aspect-square h-auto w-[38px] p-0">a</Button>
		<Button className="aspect-square h-auto w-[38px] p-0">a</Button>
	</span>
);
export default Buttons;
