
interface IinputConfig {
    syntax:{
        eslint:boolean,
        jsx:boolean
    }
}

const ineedj = (inputConfig:IinputConfig) => {
    const finalConfig = [];

    // if(inputConfig.)


}
export default ineedj